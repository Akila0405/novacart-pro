const toast=document.getElementById('toast');function showToast(m){if(!toast)return;toast.textContent=m;toast.classList.add('show');setTimeout(()=>toast.classList.remove('show'),2200)}
const mt=document.querySelector('.menu-toggle'),nl=document.querySelector('.nav-links');if(mt)mt.addEventListener('click',()=>nl.classList.toggle('open'));
document.addEventListener('click',e=>{const b=e.target.closest('[data-add]');if(b){const p=JSON.parse(b.dataset.add);fetch('cart_api.php',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action:'add',...p})}).then(r=>r.json()).then(d=>{document.getElementById('cart-count').textContent=d.count;showToast('Added to cart')})}const w=e.target.closest('[data-wish]');if(w){let a=JSON.parse(localStorage.getItem('wishlist')||'[]');const p=JSON.parse(w.dataset.wish);if(!a.find(x=>x.id==p.id))a.push(p);localStorage.setItem('wishlist',JSON.stringify(a));showToast('Saved to wishlist')}});
const q=document.querySelector('#shop-search');if(q)q.addEventListener('input',()=>{const s=q.value.toLowerCase();document.querySelectorAll('.product-card').forEach(c=>c.style.display=c.dataset.name.includes(s)?'':'none')});
